import { useState } from "react";
import "./calendar.css";

function Calendar() {
  // const [count, setCount] = useState(0)

  return (
    <div>
      <h1>Calendar</h1>
    </div>
  );
}

export default Calendar;
